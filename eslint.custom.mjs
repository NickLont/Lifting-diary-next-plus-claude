/**
 * Custom ESLint rules for this project.
 * Add your project-specific rule overrides here.
 */

const customRules = {
  rules: {
    // Warn on camelcase violations (allows snake_case like Geist_Mono)
    camelcase: 'warn',
  },
}

export default customRules
